⎕io ← 0
count ← {+/4(≢⍵)⍴'ACGT'∘.=⍵}
count_t ← +/((4,(≢⊢))⍴'ACGT'∘.=⊢)
gc ← { (((+/⍵='G') + (+/⍵='C'))÷≢⍵)×100 }
gc_t ← (≢⊢)÷⍨100×((+/'G'=⊢) + (+/'C'=⊢)) 
magic ← {~0∊((⊃(+/⍵))=((+/⍵),(+/⍉⍵),((⍴⍴⍵)⍴(+/+/(⍵×(⍵=⍉⍵)))),((⍴⍴⍵)⍴(+/+/(⌽⍵×(⌽⍵)=(⍉⌽⍵))))))}
magic_t ← ~0∊((⊃(+/⊢))=((+/⊢),(+/⍉),(+/(+/(((⍉⊢)=⊢)×⊢))),(+/(+/(((⍉(⌽⊢))=(⌽⊢))×(⌽⊢))))))
